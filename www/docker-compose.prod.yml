services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: iconip-2026-ctf-www:prod
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
